To compile on command line:

  $ pdflatex report

If you added new citations with \cite{...} that you haven't used before, then:

  $ pdflatex report
  $ bibtex report
  $ pdflatex report
  $ pdflatex report

Yes, it takes multiple times to get it.
